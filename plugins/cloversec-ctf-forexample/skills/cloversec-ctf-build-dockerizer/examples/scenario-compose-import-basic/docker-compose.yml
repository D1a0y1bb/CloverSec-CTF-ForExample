version: "3.8"

services:
  web1:
    build:
      context: ../python-loopback-ssrf-basic
    ports:
      - "15000:5000"
    depends_on:
      - redis1

  redis1:
    image: redis:7.2-alpine
    ports:
      - "16379:6379"
