## 1 题目设计部署信息

### 1.1 题目名称
Web-留言板 1.0

### 1.2 题目描述
这是一个基于 Flask 的留言板应用，题目重点在于识别服务端模板注入链路并获取服务器敏感文件。

### 1.3 题目难度
中等

### 1.4 考察信息
- 题目分类候选：`Web`
- 题目目标：通过模板注入读取服务端 `/flag` 获取最终结果。
- SSTI
- 文件读取

### 1.5 旗帜信息
- Flag 类型：动态Flag
- 静态 Flag：[题目Flag待补充]
- 容器内 Flag 路径固定为 `/flag`，权限建议为 `400`。
- 若平台启用动态 Flag，请在容器启动阶段完成写入，并确保业务进程只能按预期路径读取。
- 如存在 `changeflag.sh` 或平台注入逻辑，请在交付前复核动态 Flag 是否真正生效。
- 模板版不补全未知 Flag，需由出题人根据实际题目补写。

### 1.6 附件情况
● `writeup.md`（Writeup 或人工草稿）

### 1.7 部署方式
#### 1.7.1 Dockerfile 构建启动
```bash
docker build -t web-1-0:latest .
docker run -d -p [端口]:[端口] web-1-0:latest /start.sh
```

#### 1.7.2 Tar 镜像包导入启动
```bash
docker load -i web-1-0.tar
docker run -d -p [端口]:[端口] web-1-0:latest /start.sh
```

### 1.8 非预期测试
- [请补充非预期利用点、误报点或额外测试结论]

## 2 HUB上传部分&题解信息

### 2.1 题目标题
```plain
Web-留言板 1.0
```

### 2.2 题目内容
```plain
这是一个基于 Flask 的留言板应用，题目重点在于识别服务端模板注入链路并获取服务器敏感文件。
```

### 2.3 Flag类型
```plain
动态Flag
```

### 2.4 题目Flag
```plain
[题目Flag待补充]
```

### 2.5 题目来源
```plain
原创
```

### 2.6 题目分类
```plain
Web
```

### 2.7 题目分值
```plain
500
```

### 2.8 题目等级
```plain
5级 ★★★
```

### 2.9 题目类型
```plain
附件型
```

### 2.10 资源等级
```plain
4
```

### 2.11 题目备注
```plain
附件型题目 / 无独立容器启动要求 / 请结合附件使用方式补充说明
```

---

### 2.12 题目解答
#### 2.12.1 前置知识
- Flask 基础
- Jinja2 模板语法

#### 2.12.2 考察知识点
- SSTI
- 文件读取

#### 2.12.3 解题工具
- Burp Suite
- Python3

#### 2.12.4 题目目标
通过模板注入读取服务端 `/flag` 获取最终结果。

#### 2.12.5 解题步骤
1、信息收集：识别入口页面、目录、接口与可疑参数。
2、漏洞定位：结合源码或 WP 确认利用点与触发条件。
3、利用链构造：记录关键请求、关键脚本与回显点。
4、Flag 获取：说明最终读取或计算 Flag 的路径。

#### 2.12.6 Flag
[题目Flag待补充]
说明：当前判断为 `动态Flag`。

### 2.13 添加关键字
Web、中等、动态Flag

### 2.14 上传附件
● `[待确认上传附件]`（请结合实际题目产物补充）
