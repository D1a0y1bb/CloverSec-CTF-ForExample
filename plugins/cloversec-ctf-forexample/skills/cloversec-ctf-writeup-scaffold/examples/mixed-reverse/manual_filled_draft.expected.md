## 1 题目设计部署信息

### 1.1 题目名称
Reverse-序列号校验

### 1.2 题目描述
附件提供一个序列号校验程序，需要逆向分析校验逻辑，恢复正确输入并拿到 flag。

### 1.3 题目难度
[简单/中等/困难]

### 1.4 考察信息
- 题目分类候选：`Reverse`
- 题目目标：完成程序逻辑还原、关键校验点定位并恢复 Flag。
- 字符串交叉校验
- 逆向逻辑恢复

### 1.5 旗帜信息
- Flag 类型：[静态Flag/动态Flag]
- 静态 Flag：[题目Flag待补充]
- 容器内 Flag 路径固定为 `/flag`，权限建议为 `400`。
- 若平台启用动态 Flag，请在容器启动阶段完成写入，并确保业务进程只能按预期路径读取。
- 如存在 `changeflag.sh` 或平台注入逻辑，请在交付前复核动态 Flag 是否真正生效。

### 1.6 附件情况
● `challenge.yaml`（题目结构化配置）
● `wp.md`（Writeup 或人工草稿）

### 1.7 部署方式
#### 1.7.1 Dockerfile 构建启动
```bash
docker build -t reverse:latest .
docker run -d -p [端口]:[端口] reverse:latest /start.sh
```

#### 1.7.2 Tar 镜像包导入启动
```bash
docker load -i reverse.tar
docker run -d -p [端口]:[端口] reverse:latest /start.sh
```

### 1.8 非预期测试
- [请补充非预期利用点、误报点或额外测试结论]

## 2 HUB上传部分&题解信息

### 2.1 题目标题
```plain
Reverse-序列号校验
```

### 2.2 题目内容
```plain
附件提供一个序列号校验程序，需要逆向分析校验逻辑，恢复正确输入并拿到 flag。
```

### 2.3 Flag类型
```plain
[静态Flag/动态Flag]
```

### 2.4 题目Flag
```plain
[题目Flag待补充]
```

### 2.5 题目来源
```plain
搬运
```

### 2.6 题目分类
```plain
Reverse
```

### 2.7 题目分值
```plain
[题目分值待补充]
```

### 2.8 题目等级
```plain
[简单/中等/困难]
```

### 2.9 题目类型
```plain
环境型
```

### 2.10 资源等级
```plain
8
```

### 2.11 题目备注
```plain
环境型题目 / 请补充实际监听端口、启动方式与平台录入注意事项
```

---

### 2.12 题目解答
#### 2.12.1 前置知识
- 熟悉 Reverse 题型常见分析方法

#### 2.12.2 考察知识点
- 字符串交叉校验
- 逆向逻辑恢复

#### 2.12.3 解题工具
- IDA Free
- x64dbg

#### 2.12.4 题目目标
完成程序逻辑还原、关键校验点定位并恢复 Flag。

#### 2.12.5 解题步骤
1、静态分析：定位关键函数、字符串和控制流。
2、动态验证：说明调试、断点或补丁策略。
3、逻辑还原：记录关键算法、校验逻辑或解密过程。
4、Flag 获取：给出还原出的结果。
补充说明：当前草稿只按题型给出最小引导，详细利用链仍需人工复核补写。

#### 2.12.6 Flag
[题目Flag待补充]

### 2.13 添加关键字
Reverse

### 2.14 上传附件
● `challenge.yaml`（建议随题目归档或作为平台附件上传）
